{%- if prime.utm -%}
<a href="javascript:utm({{ prime.utm }})">
{%- endif -%}
<span class="{{ prime.isSG ? 'sophie-germain ' : '' }}{{ prime.isTwin ? 'twin ' : '' }}{{ prime.woodall ? 'woodall' : '' }}" title="{{ prime.woodall ? 'Woodall: ' ~ prime.woodall : '' }}">{{ prime.n }}</span>
{%- if prime.utm -%}</a>{%- endif -%}
{%- if prime.comments %}<a class="comment" title="{{ prime.comments|join('\n') }}">*</a>{% endif -%}
