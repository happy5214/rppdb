{% if prime.utm %}<a href="javascript:utm({{ prime.utm }})"><span class="{{ prime.isSG ? 'sophie-germain ' : '' }}{{ prime.isTwin ? 'twin ' : '' }}{{ prime.woodall ? 'woodall' : '' }}" title="{{ prime.woodall ?: '' }}">{{ prime.n }}</span></a>{% else %}<span class="{{ prime.isSG ? 'sophie-germain ' : '' }}{{ prime.isTwin ? 'twin ' : '' }}{{ prime.woodall ? 'woodall' : '' }}" title="{{ prime.woodall ?: '' }}">{{ prime.n }}</span>{% endif %}{% if prime.comments %}<a class="comment" title="{{ prime.comments|join('\n') }}">*</a>{% endif %}
